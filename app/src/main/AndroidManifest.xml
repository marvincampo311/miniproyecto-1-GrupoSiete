<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- CORRECCIÓN DEL ERROR: Usar USE_BIOMETRIC en lugar de USE_FINGERPRINT (deprecated) -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/inventory_widget"
        android:label="@string/app_name"
        android:roundIcon="@drawable/inventory_widget"
        android:supportsRtl="true"
        android:theme="@style/Theme.Miiproyecto1"
        tools:targetApi="31">

        <!-- ACTIVIDAD PRINCIPAL (LOGIN CON BIOMETRÍA) (HU 2.0) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Miiproyecto1.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ACTIVIDAD DE HOME/INVENTARIO (HU 3.0) -->
        <activity
            android:name=".HomeActivity"
            android:exported="false"
            android:theme="@style/Theme.Miiproyecto1.NoActionBar"
            />

        <activity
            android:name=".AddProductActivity"
            android:exported="true"
            android:theme="@style/Theme.Miiproyecto1.NoActionBar" />

        <activity
            android:name=".ProductDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.Miiproyecto1.NoActionBar" />

        <activity
            android:name=".EditProductActivity"
            android:exported="false"
            android:theme="@style/Theme.Miiproyecto1.NoActionBar" />


        <!-- PROVEEDOR DE WIDGET (HU 1.0) -->
        <receiver
            android:name=".InventoryWidgetProvider"
            android:exported="true">

            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/app_widget_info" />
        </receiver>
    </application>

</manifest>